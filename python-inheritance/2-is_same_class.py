#!/usr/bin/python3
"""a function that returns True if the object is
 same type as class ;
 otherwise False"""


def is_same_class(obj, a_class):
    """a function that returns True if the object
    is same type as  class ;
    otherwise False"""
    if type(obj) == a_class:
            return True
    else:
            return False
